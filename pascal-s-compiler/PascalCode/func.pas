program example(input,output);
    const time =+2;sfkj=1;
    var  x,y : array [1..100]of integer;sd:real;i,s:integer;
    
    function gcd(var a,b:integer):integer; 
        const time = 1;
        var x,y:{≤‚ ‘≤‚ ‘}integer;
        begin
            if b=0 then gcd:=a
            else gcd:=gcd(a mod b)
        end;

    begin
        x[1,2]:=0;
        {read;}
        {read(x, y);}
        for i:=1 to 10 do 
            s:=s+i;
        {write(gcd(x, y))}
end.
