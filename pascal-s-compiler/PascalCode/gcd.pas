program example(input,output);
    const time =+2;sfkj=1;
    var  x,y : array [1..100]of integer;sd:real;i,s:integer;c:char;
    
    function gcd(a,b:integer):integer; 
        const time = 1;
        var x,y:{测试测试}integer;
        begin
            if b=0 then gcd:=a
            else gcd:=gcd(b, a mod b)
        end;

    begin
        x[1]:=0;
        {read;}
        {read(x, y);}
        for i:=1 to 10 do 
            s:=s+i;
        {write(gcd(x, y))}
end.
