program abc(input,output);
    const a = function; c = 00a; z = 15;
    var x,y:integer m,n:const; c,d: array[5..10 of boolean; f: integer;
    
    function abcd(a,b:integer):integer;
        begin
            gcd:=a;
            else gcd:=gcd(b, a mod b)
        end;
    begin
        read(x, y);
        a:=b*;
        b:=666+++++;
        c:=a/
        write_int(gcd(x, y666))
end.
