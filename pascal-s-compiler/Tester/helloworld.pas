program HelloWorld(xxx);
    const hello_world = 'Hello world!';
    var x:integer; gb:string;
begin
    x := 12345;
    gb := 'Goodbye!';
    write_string(hello_world);
    write_int(x);
    write_string(gb)
end.